<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    app:startDestination="@id/welcomeFragment">

    <fragment
        android:id="@+id/welcomeFragment"
        android:name="com.example.juegodecolores.WelcomeFragment">
        <action android:id="@+id/action_welcome_to_game" app:destination="@id/gameFragment"/>
    </fragment>

    <fragment
        android:id="@+id/gameFragment"
        android:name="com.example.juegodecolores.GameFragment">
        <argument android:name="score" app:argType="integer" />
        <action android:id="@+id/action_game_to_result" app:destination="@id/resultFragment"/>
    </fragment>

    <fragment
        android:id="@+id/resultFragment"
        android:name="com.example.juegodecolores.ResultFragment">
        <action android:id="@+id/action_result_to_game" app:destination="@id/gameFragment"/>
    </fragment>
</navigation>