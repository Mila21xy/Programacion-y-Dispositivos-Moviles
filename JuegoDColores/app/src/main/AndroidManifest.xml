<?xml version="1.0" encoding="utf-8"?>

<!-- Declaración del archivo de manifiesto principal de la aplicación -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Elemento <application> define los atributos generales de la aplicación -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JuegoDColores"
        tools:targetApi="31">

    <!-- Actividad principal de la aplicación -->
    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:screenOrientation="portrait"
        tools:ignore="DiscouragedApi">  <!-- Ignora advertencias específicas del Lint -->

    <!-- Filtro de intent que define esta actividad como la principal y lanzable desde el launcher -->
    <intent-filter>
        <action android:name="android.intent.action.MAIN" />  <!-- Acción principal -->
        <category android:name="android.intent.category.LAUNCHER" />  <!-- Categoría para lanzador -->
    </intent-filter>
</activity>
    </application>

    </manifest>
